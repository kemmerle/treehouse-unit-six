extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 Select Projects

      p.lead.text-light
        | As a TalentPath intern, I have completed five
        |projects, in which I honed my skills in DOM traversal, list pagination,
        |form validation, object-oriented programming, and API requests.
        //Pipes in pug allow for line breaks in larger blocks of text, especially
        //useful for something like a small bio.

  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //Here I use the pug 'each' method to iterate over the projects property
      //I passed down in my routes folder. For each project, I specified that
      //pug should display its thumbnail and name. 
      each project in projects
        .cell
          a(href= '/projects/' + project.id)
            img.thumbnail(src='' + project.project_thumbnail)
            h5 #{project.project_name}
